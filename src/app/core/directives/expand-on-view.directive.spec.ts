import { ExpandOnViewDirective } from './expand-on-view.directive';

describe('ExpandOnViewDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandOnViewDirective();
    expect(directive).toBeTruthy();
  });
});
